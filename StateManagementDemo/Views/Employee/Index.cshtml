@model IEnumerable<StateManagementDemo.Models.EmpModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<h1>@ViewBag.mysessionID</h1>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Empid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th></th>
    </tr>
    s
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.ActionLink("Select Employee", "Select", new { id = item.Empid })

            </td>
            <td>
                @Html.ActionLink("EditEmployee", "Edit", new { id = item.Empid })
            </td>
        </tr>

    }



</table>
